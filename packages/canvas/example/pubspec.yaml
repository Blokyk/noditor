name: canvas_example
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  canvas:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  shaders:
    - packages/canvas/shaders/grid.frag